#include<iostream>
#include<cmath>

using namespace std;

int main(){
 double x=7;
 double y=3;
 double z=0;

 //z=abs(-3); or  z=abs(y-x);  turns negative to positive
 //z=sqrt(y);
 //z=sqrt(x)
 //z=min(x,y);
 //z=max(x,y);
 //z=pow(x,y);
 cout<<z<<endl;
    return 0;

}